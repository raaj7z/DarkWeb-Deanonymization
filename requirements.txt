# ─────────────────────────────────────────────
# Python dependencies for DarkWeb-Deanonymization
# Install with:  pip install -r requirements.txt
# ─────────────────────────────────────────────

# Core HTTP + Tor routing
requests[socks]==2.31.0
PySocks==1.7.1

# HTML parsing
beautifulsoup4==4.12.2

# Terminal UI
colorama==0.4.6
tqdm==4.66.1
fake-useragent==1.4.0

# TLS / crypto validation
cryptography>=42.0.0
base58==2.1.1

# Tor control port (circuit rotation)
stem==1.8.2

# PDF report generation
reportlab>=4.0.0

# Optional: ML classifier (disabled by default, but safe to install)
scikit-learn>=1.3.0

# Optional: JS rendering (only used if use_js=True)
selenium>=4.15.0
